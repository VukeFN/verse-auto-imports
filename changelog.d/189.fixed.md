**Import comments**: the comment trailing a `using` statement is no longer deleted when imports are sorted or organized, and an import line carrying a comment marker or an unclosed `<#` block opener is left where it was written.
