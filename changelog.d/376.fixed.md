**Project detection**: changes to a `.uefnproject` outside the opened workspace folders are now observed, so project identity, the path cache and asset class names refresh mid-session instead of staying stale.
