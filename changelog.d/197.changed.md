**Bundled Verse API digests**: refreshed to UEFN 41.30. The LLM conversation API is renamed to AI, so `ai_session`, `ai_error` and `ai_description` now resolve and the old `llm_*` names no longer do, and the generic voice classes are replaced with Fortnite character voices. Also adds 29 Fortnite weapons and devices, plus `available_input_devices` and `PointerZoom`.
