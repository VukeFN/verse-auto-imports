**Auto import**: a newly added import is now written below an import that shares its line with another statement, instead of above it, so an import that needs that line's module in scope resolves where it lands
