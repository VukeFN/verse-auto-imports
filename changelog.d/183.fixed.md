**Path conversion**: converting an import whose statement also appears elsewhere in the file, commented out above it say, now rewrites the line the CodeLens sits on rather than the first line reading the same.
