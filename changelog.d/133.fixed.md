**Failed edits are no longer reported as successes**: when a document edit is rejected, because the file changed underneath the extension or is read-only, adding a single import, auto-importing and Optimize Imports now show a warning instead of a success message. Optimize Imports also no longer saves the document when its edit did not apply, which previously wrote the unorganized content back to disk.
