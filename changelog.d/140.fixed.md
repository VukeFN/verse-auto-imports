**Optimize Imports**: a file header comment now stays above the rebuilt import block instead of being pushed into the middle of the file, and a comment written directly above an import travels with that import when the block is sorted. Adding an import with sorting turned off no longer inserts it above the header either.
