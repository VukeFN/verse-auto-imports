**Build and environment in the logs**: the activation line now names the extension version, VS Code version, platform and workspace shape, and the exported debug log repeats them in its header along with the settings that change import behavior. An exported session can be tied back to the build that produced it.
