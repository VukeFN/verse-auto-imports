**Auto import and CodeLens**: a pending auto-import can no longer edit a file after the extension is disabled, the CodeLens provider stops handling keystrokes once it is gone, and a path conversion lens no longer vanishes under the cursor.
