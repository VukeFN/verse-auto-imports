**Path conversion CodeLens**: a `using` inside a block comment or indented in a module body no longer gets a conversion lens or is touched by the convert-all commands, while a bare folder import now does.
