**Path conversion CodeLens**: a `using` inside a `<# ... #>` block comment, or one indented inside a module body, no longer gets a Use relative path / Use absolute path lens, and no longer counts towards the convert-all lens. The provider and the converter now read the same import scanner the rest of the extension uses, so acting on a lens can no longer edit a line inside a comment.
