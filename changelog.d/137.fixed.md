**Project path cache**: clearing the cache - through the Clear Project Path Cache command, or automatically when UEFN rewrites the .uefnproject - no longer crashes a file update that is already running. The update is discarded cleanly instead of failing part-way and leaving stale declarations in workspace storage.
