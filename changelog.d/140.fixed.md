**Header comments**: a file header comment now stays above the import block when imports are organized or a new import is added, and a comment directly above an import travels with it when the block is sorted.
