**Notifications**: routine confirmations (snooze, menu picks, cache rebuild, path conversions, optimize imports) now show as 3-second status bar messages instead of notification toasts, and Show Cache Status reports one non-modal line instead of blocking the window.
