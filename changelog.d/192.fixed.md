**Optimize Imports**: a file holding both a comment-anchored import and an ordinary import of the same path no longer keeps both - the ordinary one is removed, unless the file also imports a module by a bare or dotted name.
