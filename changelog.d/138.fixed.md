**Project path cache setting**: turning `cache.enableProjectCache` off now really disables the cache - the rebuild, clear and status commands report it as off, a stored cache is dropped, and toggling the setting offers the window reload it needs.
